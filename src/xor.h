#ifndef xor_h
#define xor_h

/**
xor:
  Performs bitwise operation 'a' XOR 'b' for a block of size 'len' and stores
  it in the specified 'dest' array.
*/ 
void xor(char* a, char* b, int len, char* dest);

#endif
